# Полный API документ - Все Endpoints

**Base URL:** `http://localhost:3000/api`

---

## 📚 Таблица содержания

1. [Классы (Classes)](#-классы-classes)
2. [Предметы (Subjects)](#-предметы-subjects)
3. [Родители (Parents)](#-родители-parents)
4. [Домашнее задание (Homework)](#-домашнее-задание-homework)
5. [Дни (Days)](#-дни-days)
6. [Журналы (Journals)](#-журналы-journals)
7. [Уроки (Lessons)](#-уроки-lessons)
8. [Материалы (Materials)](#-материалы-materials)
9. [Данные студентов (Student Data)](#-данные-студентов-student-data)
10. [Расписание (Timetables)](#-расписание-timetables)
11. [Родители студентов (Student Parents)](#-родители-студентов-student-parents)
12. [Роли (Roles)](#-роли-roles)
13. [Пользователи (Users)](#-пользователи-users)
14. [Роли пользователей (User Roles)](#-роли-пользователей-user-roles)
15. [Учителя (Teachers)](#-учителя-teachers)

---

## 🏫 Классы (Classes)

**Base:** `/api/classes`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить все классы | — |
| GET | `/:id` | Получить класс по ID | — |
| POST | `/` | Создать новый класс | `{ "name", "journalId", "mainTeacherId" }` |
| PATCH | `/:id` | Обновить класс | `{ "name", "mainTeacherId" }` |
| DELETE | `/:id` | Удалить класс | — |

### Примеры:
```bash
# Получить все классы
curl GET http://localhost:3000/api/classes

# Создать класс
curl -X POST http://localhost:3000/api/classes \
  -H "Content-Type: application/json" \
  -d '{ "name": "10-A", "journalId": 1, "mainTeacherId": 5 }'
```

---

## 📖 Предметы (Subjects)

**Base:** `/api/subjects`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить все предметы | — |
| POST | `/` | Создать новый предмет | `{ "name", "program" }` |
| DELETE | `/:id` | Удалить предмет | — |

### Примеры:
```bash
# Получить все предметы
curl GET http://localhost:3000/api/subjects

# Создать предмет
curl -X POST http://localhost:3000/api/subjects \
  -H "Content-Type: application/json" \
  -d '{ "name": "Математика", "program": "Основная программа" }'
```

---

## 👥 Родители (Parents)

**Base:** `/api/parents`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить всех родителей | — |
| GET | `/:id` | Получить родителя по ID | — |
| POST | `/` | Создать нового родителя | `{ "name", "surname", "patronym", "phone" }` |
| PATCH | `/:id` | Обновить родителя | `{ "name", "surname", "patronym", "phone" }` |
| DELETE | `/:id` | Удалить родителя | — |

### Примеры:
```bash
# Создать родителя
curl -X POST http://localhost:3000/api/parents \
  -H "Content-Type: application/json" \
  -d '{ "name": "Иван", "surname": "Петров", "patronym": "Сергеевич", "phone": "+79991234567" }'
```

---

## 📝 Домашнее задание (Homework)

**Base:** `/api/homework`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить все ДЗ | — |
| GET | `/:id` | Получить ДЗ по ID | — |
| GET | `/by-student-or-class` | ДЗ по студенту или классу | — |
| GET | `/for-tomorrow` | ДЗ на завтра | — |
| POST | `/` | Создать ДЗ | `{ "name", "description", "dueDate", "subjectId", "classId" }` |
| PATCH | `/:id` | Обновить ДЗ | `{ "name", "description", "dueDate" }` |
| DELETE | `/:id` | Удалить ДЗ | — |

### Примеры:
```bash
# Создать домашнее задание
curl -X POST http://localhost:3000/api/homework \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Задача 1",
    "description": "Решить примеры",
    "dueDate": "2024-12-20",
    "subjectId": 1,
    "classId": 1
  }'
```

---

## 🕐 Дни (Days)

**Base:** `/api/days`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить все дни | — |
| GET | `/:id` | Получить день по ID | — |
| POST | `/` | Создать день | `{ "dayName", "dayTime", "dayWeekday" }` |
| PATCH | `/:id` | Обновить день | `{ "dayName", "dayTime", "dayWeekday" }` |
| DELETE | `/:id` | Удалить день | — |

### Примеры:
```bash
# Создать день
curl -X POST http://localhost:3000/api/days \
  -H "Content-Type: application/json" \
  -d '{ "dayName": "Понедельник", "dayTime": "09:00:00", "dayWeekday": "Monday" }'
```

---

## 📚 Журналы (Journals)

**Base:** `/api/journals`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить все журналы | — |
| GET | `/:id` | Получить журнал по ID | — |
| POST | `/` | Создать журнал | `{ "teacherId", "name" }` |
| PATCH | `/:id` | Обновить журнал | `{ "teacherId", "name" }` |
| DELETE | `/:id` | Удалить журнал | — |

### Примеры:
```bash
# Создать журнал
curl -X POST http://localhost:3000/api/journals \
  -H "Content-Type: application/json" \
  -d '{ "teacherId": 5, "name": "Журнал 10-А класса" }'
```

---

## 🎓 Уроки (Lessons)

**Base:** `/api/lessons`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить все уроки | — |
| GET | `/:id` | Получить урок по ID | — |
| POST | `/` | Создать урок | `{ "name", "className", "subjectId", "materialId", "teacherId", "date" }` |
| PATCH | `/:id` | Обновить урок | `{ "name", "className", "subjectId", "materialId", "teacherId", "date" }` |
| DELETE | `/:id` | Удалить урок | — |

### Примеры:
```bash
# Создать урок
curl -X POST http://localhost:3000/api/lessons \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Уроки Пифагора",
    "className": "10-A",
    "subjectId": 1,
    "materialId": 5,
    "teacherId": 3,
    "date": "2024-12-20"
  }'
```

---

## 📄 Материалы (Materials)

**Base:** `/api/materials`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить все материалы | — |
| GET | `/:id` | Получить материал по ID | — |
| POST | `/` | Создать материал | `{ "name", "description", "link" }` |
| PATCH | `/:id` | Обновить материал | `{ "name", "description", "link" }` |
| DELETE | `/:id` | Удалить материал | — |

### Примеры:
```bash
# Создать материал
curl -X POST http://localhost:3000/api/materials \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Презентация 1",
    "description": "Введение в алгебру",
    "link": "https://example.com/presentation.pdf"
  }'
```

---

## 📊 Данные студентов (Student Data)

**Base:** `/api/studentdata`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить все данные | — |
| GET | `/:id` | Получить данные по ID | — |
| POST | `/` | Создать данные | `{ "journalId", "studentId", "lesson", "mark", "status", "note" }` |
| PATCH | `/:id` | Обновить данные | `{ "journalId", "studentId", "lesson", "mark", "status", "note" }` |
| DELETE | `/:id` | Удалить данные | — |

### Примеры:
```bash
# Создать данные студента
curl -X POST http://localhost:3000/api/studentdata \
  -H "Content-Type: application/json" \
  -d '{
    "journalId": 1,
    "studentId": 10,
    "lesson": 1,
    "mark": 5,
    "status": "present",
    "note": "Отлично"
  }'
```

---

## 📅 Расписание (Timetables)

**Base:** `/api/timetables`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить все расписания | — |
| GET | `/:id` | Получить расписание по ID | — |
| POST | `/` | Создать расписание | `{ "name", "classId" }` |
| PATCH | `/:id` | Обновить расписание | `{ "name", "classId" }` |
| DELETE | `/:id` | Удалить расписание | — |

### Примеры:
```bash
# Создать расписание
curl -X POST http://localhost:3000/api/timetables \
  -H "Content-Type: application/json" \
  -d '{ "name": "Расписание 10-А", "classId": 1 }'
```

---

## 👨‍👩‍👧 Родители студентов (Student Parents)

**Base:** `/api/studentparents`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/:studentId` | Получить родителей студента | — |
| POST | `/assign` | Назначить родителя студенту | `{ "studentId", "parentId" }` |
| DELETE | `/unassign` | Удалить родителя у студента | `{ "studentId", "parentId" }` |

### Примеры:
```bash
# Назначить родителя студенту
curl -X POST http://localhost:3000/api/studentparents/assign \
  -H "Content-Type: application/json" \
  -d '{ "studentId": 10, "parentId": 5 }'
```

---

## 🔐 Роли (Roles)

**Base:** `/api/roles`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить все роли | — |
| GET | `/:id` | Получить роль по ID | — |
| POST | `/` | Создать роль | `{ "roleName" }` |
| PATCH | `/:id` | Обновить роль | `{ "roleName" }` |
| DELETE | `/:id` | Удалить роль | — |

---

## 👤 Пользователи (Users)

**Base:** `/api/users`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить всех пользователей | — |
| GET | `/:id` | Получить пользователя по ID | — |
| POST | `/` | Создать пользователя | `{ "username", "email", "password" }` |
| PATCH | `/:id` | Обновить пользователя | `{ "username", "email", "password" }` |
| DELETE | `/:id` | Удалить пользователя | — |
| POST | `/reset-password` | Сбросить пароль | `{ "userId", "newPassword" }` |

---

## 👥 Роли пользователей (User Roles)

**Base:** `/api/userroles`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/:userId` | Получить роли пользователя | — |
| POST | `/assign` | Назначить роль | `{ "userId", "roleId" }` |
| DELETE | `/remove` | Удалить роль | `{ "userId", "roleId" }` |

---

## 👨‍🏫 Учителя (Teachers)

**Base:** `/api/teacher`

| Метод | Endpoint | Описание | Body |
|-------|----------|---------|------|
| GET | `/` | Получить всех учителей | — |
| GET | `/:id` | Получить учителя по ID | — |
| GET | `/with-classes` | Учителя с их классами | — |
| GET | `/salary?teacherId=1&fromDate=2024-01-01&toDate=2024-12-31` | Зарплата учителя | — |
| POST | `/` | Создать учителя | `{ "name", "surname", "patronym", "phone" }` |
| PATCH | `/:id` | Обновить учителя | `{ "name", "surname", "patronym", "phone" }` |
| DELETE | `/:id` | Удалить учителя | — |

---

## 🔍 Коды ответов

| Код | Описание |
|-----|---------|
| 200 | OK - Успешный GET/PATCH запрос |
| 201 | Created - Успешное создание (POST) |
| 400 | Bad Request - Ошибка валидации или БД |
| 404 | Not Found - Ресурс не найден |
| 500 | Internal Server Error - Ошибка сервера |

---

## 📋 Структура ошибок

Все ошибки возвращаются в формате:
```json
{
  "error": "Описание ошибки"
}
```

---

## ✅ Всего адаптировано

- ✅ 16 моделей
- ✅ 16 сервисов
- ✅ 16 контролеров
- ✅ 16 роутеров
- ✅ 50+ API endpoints

---

**Дата**: 2024 M12 16  
**Статус**: ✅ Полностью готово

